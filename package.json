{
    "name": "libcpprime-docs",
    "private": true,
    "type": "module",
    "scripts": {
        "docs:dev": "vitepress dev docs",
        "docs:build": "vitepress build docs",
        "docs:preview": "vitepress preview docs",
        "deploy": "wrangler deploy"
    },
    "devDependencies": {
        "vitepress": "^1.6.4",
        "wrangler": "^3.114.0"
    }
}
